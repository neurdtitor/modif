diff --git a/README.md b/README.md
index 026d713..e1a6225 100644
--- a/README.md
+++ b/README.md
@@ -112,7 +112,8 @@ the leader key itself is `LEADER_KEY` (change it to any key, e.g. `\`).
 
 | Leader chord  | Action                          |
 |---------------|---------------------------------|
-| `<Space>f`    | fuzzy finder                    |
+| `<Space>f`    | fuzzy finder (files)            |
+| `<Space>b`    | fuzzy finder (buffers)          |
 | `<Space>t`    | toggle terminal pane            |
 | `<Space>w`    | switch pane focus               |
 | `<Space>s`    | save                            |
diff --git a/config.h b/config.h
index 93b47d0..268ba49 100644
--- a/config.h
+++ b/config.h
@@ -23,6 +23,7 @@ static const KeyBind global_keys[] = {
 /* Leader bindings: LEADER_KEY then one of these. */
 static const KeyBind leader_keys[] = {
     { 'f', cmd_fuzzy },
+    { 'b', cmd_buf_fuzzy },
     { 't', cmd_toggle_term },
     { 'w', cmd_switch_focus },
     { 's', cmd_save },
diff --git a/edit.c b/edit.c
index c86b5df..a9ede7b 100644
--- a/edit.c
+++ b/edit.c
@@ -740,7 +740,7 @@ static void cmd_key(int key) {
         break;
     case ENTER:
         cmd_execute();
-        E.mode = MODE_NORMAL;
+        if (E.mode == MODE_CMD) E.mode = MODE_NORMAL;
         break;
     case BACKSPACE:
     case CTRL('H'):
@@ -805,10 +805,33 @@ void cmd_fuzzy(void) {
     E.fqlen = 0;
     E.fq[0] = '\0';
     E.fsel = 0;
+    E.fbuf = 0;
     fuzzy_build(&E);
     fuzzy_filter(&E);
 }
 
+void cmd_buf_fuzzy(void) {
+    fuzzy_free(&E);
+    E.flist = malloc((E.nb ? E.nb : 1) * sizeof(char *));
+    E.fn = E.nb;
+    E.fcap = E.nb;
+    for (size_t i = 0; i < E.nb; i++) {
+        char tmp[1024];
+        const char *n = E.blist[i].name ? E.blist[i].name : "[no name]";
+        snprintf(tmp, sizeof tmp, "%zu %s", i + 1, n);
+        E.flist[i] = strdup(tmp);
+    }
+    E.fscore = malloc((E.nb ? E.nb : 1) * sizeof(int));
+    E.fidx = malloc((E.nb ? E.nb : 1) * sizeof(size_t));
+    E.fbuf = 1;
+    E.fqlen = 0;
+    E.fq[0] = '\0';
+    E.fsel = 0;
+    E.mode = MODE_FUZZY;
+    E.focus = FOCUS_EDIT;
+    fuzzy_filter(&E);
+}
+
 /* ------------------------------- fuzzy keys ------------------------------- */
 
 static void fuzzy_key(int key) {
@@ -825,6 +848,14 @@ static void fuzzy_key(int key) {
         break;
     case ENTER:
         if (E.fcount && E.fsel < (int)E.fcount) {
+            if (E.fbuf) {
+                size_t bi = E.fidx[E.fsel];
+                fuzzy_free(&E);
+                buf_sync();
+                buf_load(bi);
+                E.mode = MODE_NORMAL;
+                return;
+            }
             char path[1024];
             snprintf(path, sizeof path, "%s", E.flist[E.fidx[E.fsel]]);
             fuzzy_free(&E);
@@ -986,7 +1017,9 @@ void edit_status(void) {
         return;
     }
     if (E.mode == MODE_FUZZY) {
-        snprintf(E.status, sizeof E.status, "FUZZY: %s  (%zu matches)",
+        snprintf(E.status, sizeof E.status,
+                 E.fbuf ? "BUFFERS: %s  (%zu matches)"
+                        : "FUZZY: %s  (%zu matches)",
                  E.fq, E.fcount);
         return;
     }
@@ -1061,6 +1094,7 @@ void edit_init(void) {
     E.fcount = 0;
     E.fqlen = 0;
     E.fsel = 0;
+    E.fbuf = 0;
     pp_idx = (size_t)-1;
     leader = 0;
     count_reset();
diff --git a/edit.h b/edit.h
index c7efd46..1e3c048 100644
--- a/edit.h
+++ b/edit.h
@@ -46,6 +46,7 @@ typedef struct {
     char fq[128];
     size_t fqlen;
     int fsel;
+    int fbuf;         /* 1 = fuzzy matching open buffers, 0 = files */
 } Editor;
 
 extern Editor E;
diff --git a/fuzzy.c b/fuzzy.c
index 49c295f..4c09ba1 100644
--- a/fuzzy.c
+++ b/fuzzy.c
@@ -133,4 +133,5 @@ void fuzzy_free(Editor *E) {
     E->fidx = NULL;
     E->fcount = 0;
     E->fsel = 0;
+    E->fbuf = 0;
 }
\ No newline at end of file
diff --git a/main.c b/main.c
index 0350774..5390a05 100644
--- a/main.c
+++ b/main.c
@@ -174,7 +174,7 @@ static void render_fuzzy_line(struct abuf *ab, int y, int poptop, int poph) {
     char tmp[1024];
     int rel = y - poptop;
     if (rel == 0) {
-        snprintf(tmp, sizeof tmp, "FUZZY: %s", E.fq);
+        snprintf(tmp, sizeof tmp, E.fbuf ? "BUFFERS: %s" : "FUZZY: %s", E.fq);
     } else {
         int vis = poph - 1;
         int n = (int)E.fcount;
